---
name: {{ slug }}
type: workflow
description: {{ src.title|truncate(180, true, '...') }}
---

# Workflow: {{ src.title }}

> Extracted from {{ src.source_kind }} `{{ src.source_id }}` on {{ created_at }}{% if src.metadata.get('bc_url') %} ([source]({{ src.metadata.get('bc_url') }})){% endif %}.

## What this workflow accomplishes

{{ src.body }}

## Steps

(Edit: ordered steps, one per line. The extractor pre-fills from the source body; review and tighten.)

## How it's invoked

(Edit: on-demand command, scheduled trigger, event signal, ... whatever kicks it off.)

## Success criteria

(Edit: how you know the run completed correctly.)

## Dependencies

(Edit: other workflows / services / credentials this needs.)

{% if src.metadata.get('comments') %}
## Conversation context

{% for c in src.metadata.get('comments') -%}
- **{{ c.author }}** ({{ c.created_at[:10] }}): {{ c.body|truncate(400, true, '...') }}
{% endfor %}
{% endif %}

---

_Generated by Colaberry skill_extractor on {{ created_at }}. Source: `{{ src.source_kind }}:{{ src.source_id }}`._
