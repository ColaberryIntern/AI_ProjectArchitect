---
name: {{ slug }}
type: policy
description: {{ src.title|truncate(180, true, '...') }}
status: draft
---

# Policy: {{ src.title }}

> Extracted from {{ src.source_kind }} `{{ src.source_id }}` on {{ created_at }}{% if src.metadata.get('bc_url') %} ([source]({{ src.metadata.get('bc_url') }})){% endif %}.

## Statement

{{ src.body }}

## Scope

(Edit: who this policy applies to + which systems/processes it governs.)

## Required behavior

(Edit: bulleted list of what people / agents MUST do.)

## Forbidden behavior

(Edit: bulleted list of what people / agents MUST NOT do.)

## Enforcement

(Edit: how violations are detected and what happens when they occur.)

{% if src.metadata.get('comments') %}
## Discussion leading to this policy

{% for c in src.metadata.get('comments') -%}
- **{{ c.author }}** ({{ c.created_at[:10] }}): {{ c.body|truncate(400, true, '...') }}
{% endfor %}
{% endif %}

---

_Generated by Colaberry skill_extractor on {{ created_at }}. Status: draft until reviewed + approved by a tenant admin._
