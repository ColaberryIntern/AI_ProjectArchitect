---
name: {{ slug }}
type: report
description: {{ src.title|truncate(180, true, '...') }}
---

# Report: {{ src.title }}

> Extracted from {{ src.source_kind }} `{{ src.source_id }}` on {{ created_at }}{% if src.metadata.get('bc_url') %} ([source]({{ src.metadata.get('bc_url') }})){% endif %}.

## What this report communicates

{{ src.body }}

## Audience

(Edit: who reads this report -- exec / team / customer.)

## Cadence

(Edit: when it goes out -- daily 9am / weekly Mon / monthly first business day.)

## Sections

(Edit: the canonical sections of the report.)

1. **Headline** -- one-line state
2. **Wins** -- what shipped / closed
3. **Risks** -- what's slipping or blocked
4. **Next actions** -- what's queued for the next cycle

## Source data

(Edit: which dashboards / BC lists / scorecards feed the report.)

{% if src.metadata.get('comments') %}
## Decisions in the thread

{% for c in src.metadata.get('comments') -%}
- **{{ c.author }}** ({{ c.created_at[:10] }}): {{ c.body|truncate(400, true, '...') }}
{% endfor %}
{% endif %}

---

_Generated by Colaberry skill_extractor on {{ created_at }}._
