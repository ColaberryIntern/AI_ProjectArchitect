---
name: {{ slug }}
type: connector
description: {{ src.title|truncate(180, true, '...') }}
---

# Connector: {{ src.title }}

> Extracted from {{ src.source_kind }} `{{ src.source_id }}` on {{ created_at }}{% if src.metadata.get('bc_url') %} ([source]({{ src.metadata.get('bc_url') }})){% endif %}.

## What this connector does

{{ src.body }}

## External system

(Edit: name + URL of the system this connector talks to.)

## Auth method

(Edit: OAuth / API key / webhook secret / etc. Which vault entry it reads.)

## Endpoints used

(Edit: list the specific endpoints + HTTP methods + expected status codes.)

## Rate limits + retry policy

(Edit: e.g. "50 req/10s, exponential backoff up to 3 retries on 429".)

{% if src.metadata.get('comments') %}
## Integration notes from the thread

{% for c in src.metadata.get('comments') -%}
- **{{ c.author }}** ({{ c.created_at[:10] }}): {{ c.body|truncate(400, true, '...') }}
{% endfor %}
{% endif %}

---

_Generated by Colaberry skill_extractor on {{ created_at }}._
