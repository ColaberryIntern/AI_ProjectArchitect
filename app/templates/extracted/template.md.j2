---
name: {{ slug }}
type: template
description: {{ src.title|truncate(180, true, '...') }}
---

# Template: {{ src.title }}

> Extracted from {{ src.source_kind }} `{{ src.source_id }}` on {{ created_at }}{% if src.metadata.get('bc_url') %} ([source]({{ src.metadata.get('bc_url') }})){% endif %}.

## Purpose

{{ src.body }}

## When to use this template

(Edit: the situation that should trigger reaching for this template.)

## Template content

```
(Edit: paste the actual template body here. Use {{ '{{placeholders}}' }} for variable fields.)
```

## Required substitutions

(Edit: list every placeholder and what value goes in it.)

{% if src.metadata.get('comments') %}
## Notes from the thread

{% for c in src.metadata.get('comments') -%}
- **{{ c.author }}** ({{ c.created_at[:10] }}): {{ c.body|truncate(400, true, '...') }}
{% endfor %}
{% endif %}

---

_Generated by Colaberry skill_extractor on {{ created_at }}._
