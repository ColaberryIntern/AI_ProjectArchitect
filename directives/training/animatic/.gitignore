# Intermediate build artifacts; safe to delete, regenerated by build_animatic.py
work/

# Playwright persistent browser profile (caches Vimeo login). Do not commit —
# contains cookies, localStorage, and session state for whatever account is logged in.
.playwright-vimeo-profile/

# Voice samples + spotcheck stills (dev artifacts)
_voice_samples/
_spotcheck-*.png
_spotcheck.py
_voice_test.py

# Upload run logs
vimeo_upload_status.jsonl
