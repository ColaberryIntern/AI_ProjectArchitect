---
name: data-1-backfill-552-existing-library-items-into-multi-tenant
type: mcp
description: [Data 1] Backfill 552 existing library items into multi-tenant model
---

# MCP Server: [Data 1] Backfill 552 existing library items into multi-tenant model

> Extracted from bc_ticket `9956776470` on 2026-06-05T19:26:13Z ([source](https://app.basecamp.com/3945211/buckets/7463955/todos/9956776470)).

## What this MCP server provides

Goal: the live library has 552 items today (56 use cases, 115 skills, 17 agents, 1 prompt, 363 MCP servers, 3 capabilities) under an anonymous global workspace. Once [Auth 1] ships the multi-tenant data model, these items need correct ownership + correct approval state so they don't disappear from view.

Acceptance:

One-shot Python migration script in scripts/backfill_tenancy.py.

Default owning company for all 552 items: a colaberry tenant row created in [Admin 3].

Items previously displayed in the global library get an item_approvals row: (item_id, company_id=colaberry, status=approved, visibility=shared-public, approved_by=system-backfill, approved_at=2026-06-17).

This makes them visible to: (a) all logged-in Colaberry users under "Colaberry approved," (b) all logged-in users at other tenants under "Other company → Colaberry," (c) anonymous users (shared-public).

Idempotent: running twice produces zero duplicate rows. Use UPSERT semantics.

Dry-run mode (--dry-run flag) prints intended changes without writing.

Audit: a summary report posted to this todo as a comment when the migration runs (counts per category, any items that needed manual review).

Rollback: a paired script scripts/rollback_tenancy.py that nulls the new columns and removes backfill rows (in case something is wrong).

Implementation hints: read the existing items via the FastAPI app's ORM layer; don't write raw SQL. Run on staging first against a DB snapshot.

Depends on: [Auth 1] (schema), [Admin 3] (Colaberry tenant must exist).

## .mcp.json snippet

```json
{
  "mcpServers": {
    "data-1-backfill-552-existing-library-items-into-multi-tenant": {
      "command": "npx",
      "args": ["-y", "<package-name>"],
      "env": {}
    }
  }
}
```

## Tools exposed

(Edit: list the MCP tools this server registers and what each one does.)

## Auth / credentials

(Edit: which vault secrets this server expects.)

## Setup notes from the thread

- **CB System** (2026-06-03): Backfill executed on prod via docker compose exec app python -c 'from execution.products.library.tenancy_backfill import run; print(run())'

Result:

companies_seeded: 2 (colaberry + demo-tenant)

users_seeded: 5 (ali, ram, karun, plus 2 from initial seed)

assets_assigned_to_colaberry: 0

asset_approvals_created: 0

use_case_approvals_created: 0

Why the assigned-counts are 0 (not 552): AssetM...

---

_Generated by Colaberry skill_extractor on 2026-06-05T19:26:13Z._
